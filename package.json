{
  "name": "daily-report-vscode",
  "displayName": "Daily Report",
  "description": "VSCode extension for efficient daily report creation and management",
  "version": "1.0.0",
  "publisher": "your-publisher-name",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "dailyReport.createReport",
        "title": "Daily Report: Create Daily Report",
        "category": "Daily Report"
      },
      {
        "command": "dailyReport.addEntry",
        "title": "Daily Report: Add Report Entry",
        "category": "Daily Report"
      },
      {
        "command": "dailyReport.importCalendar",
        "title": "Daily Report: Import Today's Calendar Events",
        "category": "Daily Report"
      },
      {
        "command": "dailyReport.configureGasUrl",
        "title": "Daily Report: Configure Google Apps Script URL",
        "category": "Daily Report"
      }
    ],
    "keybindings": [
      {
        "command": "dailyReport.createReport",
        "key": "ctrl+shift+d",
        "mac": "ctrl+shift+d"
      },
      {
        "command": "dailyReport.addEntry",
        "key": "ctrl+shift+s",
        "mac": "ctrl+shift+s"
      },
      {
        "command": "dailyReport.importCalendar",
        "key": "ctrl+alt+g",
        "mac": "ctrl+alt+g"
      }
    ],
    "configuration": {
      "title": "Daily Report",
      "properties": {
        "dailyReport.reportsDirectory": {
          "type": "string",
          "default": "~/DailyReports",
          "description": "Directory path where daily reports will be saved. Use absolute path (e.g., /Users/username/Documents/Reports) or relative to home directory (e.g., Documents/Reports)"
        },
        "dailyReport.gasWebAppUrl": {
          "type": "string",
          "default": "",
          "description": "Google Apps Script Web App URL for calendar integration. Format: https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec",
          "pattern": "^https://script\\.google\\.com/macros/s/[a-zA-Z0-9_-]+/exec$",
          "patternErrorMessage": "URL must be a valid Google Apps Script Web App URL"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "dependencies": {
    "abort-controller": "^3.0.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^16.x",
    "@types/node-fetch": "^2.6.7",
    "@types/vscode": "^1.60.0",
    "typescript": "^4.4.4"
  }
}
